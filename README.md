# gtm_orphan_nodes
Finds  Orphan nodes (tags, triggers, variables) in an extracted GTM container JSON, and outputs them to copyable text -- so that any GTM user can find and remove orphan elements to reduce container space.
